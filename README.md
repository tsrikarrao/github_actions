# github_actions

This is a repository for testing GitHub Actions. It contains a simple workflow that runs on every push to the main
branch. The workflow includes a single job that prints "Hello, World!" to the console.